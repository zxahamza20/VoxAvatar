<<<<<<< HEAD
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
# bcrypt used directly for password hashing — passlib 1.7.4 (2020) is
# incompatible with bcrypt 5.x and is unmaintained, so we dropped it.
bcrypt>=4.1.0
python-dotenv==1.0.0
pydantic[email]==2.5.3
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# Redis & Caching
redis==5.0.1
hiredis==2.3.2

# AWS
boto3==1.34.25
aioboto3==12.3.0

# WebSocket
websockets==12.0
python-socketio==5.11.0

# AI/ML
# anthropic >=0.45 supports Claude 4.x models (Opus 4.7, Sonnet 4.6, Haiku 4.5)
# and the cache_control parameter used in app/services/llm.py
anthropic>=0.45.0
openai>=1.50.0
torch==2.2.0
torchvision==0.17.0
torchaudio==2.2.0
transformers==4.37.2
accelerate==0.26.1
diffusers==0.32.2
numpy==1.26.3
opencv-python==4.9.0.80
Pillow==10.2.0
librosa==0.10.1
soundfile==0.12.1
scipy==1.12.0

# STT
faster-whisper>=1.0.0

# TTS
# Chatterbox by Resemble AI — fastest open-source TTS w/ voice cloning + 23 langs
# (en, es, fr, de, it, pt, pl, tr, ru, nl, ar, zh, ja, ko, da, el, fi, he, hi,
# ms, no, sv, sw). Replaces deprecated Coqui XTTS v2.
chatterbox-tts>=0.1.4
# Edge TTS — free Microsoft neural voices used as the first fallback when
# Chatterbox is unavailable (no API key, no GPU). gTTS remains the last resort.
edge-tts>=6.1.0
gtts==2.5.1
pydub==0.25.1

# Avatar Animation
face-alignment==1.4.1
imageio==2.33.1
imageio-ffmpeg==0.4.9
scikit-image==0.22.0
kornia==0.7.1

# Video Processing
ffmpeg-python==0.2.0

# Celery
celery==5.3.6
flower==2.0.1

# Monitoring & Logging
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==6.1.0
sentry-sdk==1.40.0

# Utilities
httpx==0.26.0
aiofiles==23.2.1
tenacity==8.2.3
pyyaml==6.0.1
requests==2.31.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
aiosqlite==0.19.0  # in-memory async DB for the test suite
httpx==0.26.0  # AsyncClient for endpoint tests (also a runtime dep above)
=======

>>>>>>> f8f84853a8fec65c5b96a70fbc47ff6db7541d53
